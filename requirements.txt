# Runtime + tooling. Featherweight container stays the rule — no torch, no
# sentence-transformers; chat/embedding models are external APIs. The one
# local model is FlashRank's ~4MB ONNX reranker (optional: code degrades to
# RRF order if this install is dropped).
fastapi==0.115.6
uvicorn[standard]==0.34.0
qdrant-client==1.12.2
httpx==0.27.2
pydantic-settings==2.7.1
python-multipart==0.0.20
pymupdf==1.25.2
tiktoken==0.8.0
python-jose[cryptography]==3.3.0  # HS256 JWT minting/verification (auth)

# BYOK multi-provider LLM layer (v3): LangChain integrations + LangGraph.
# ChatOpenAI covers OpenRouter/Groq/OpenAI (same wire protocol, base_url swap).
langchain-core~=1.4
langchain-openai~=1.3
langchain-anthropic~=1.4
langchain-google-genai~=4.2
langgraph>=1.0,<2
flashrank~=0.2  # open-source cross-encoder reranker (tiny ONNX, CPU)

# Dev / CI (lint + test).
pytest==8.3.4
pytest-asyncio==0.25.2
ruff==0.9.2
