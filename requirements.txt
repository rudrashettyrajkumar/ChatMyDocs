# Runtime + tooling, pinned to majors (featherweight container — no torch, no
# sentence-transformers; embeddings/LLMs are external APIs).
fastapi==0.115.6
uvicorn[standard]==0.34.0
litellm==1.59.8
qdrant-client==1.12.2
httpx==0.27.2
pydantic-settings==2.7.1
python-multipart==0.0.20
pymupdf==1.25.2
tiktoken==0.8.0
python-jose[cryptography]==3.3.0  # HS256 JWT minting/verification (auth)

# Dev / CI (lint + test).
pytest==8.3.4
pytest-asyncio==0.25.2
ruff==0.9.2
